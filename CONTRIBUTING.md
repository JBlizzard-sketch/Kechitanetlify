# Contributing to Kechita Capital

Guidelines:
- Branch from `main` and open PRs for features.
- Run tests before pushing.
- Keep code modular in `services/` and `lib/`.
- Use environment variables for secrets; don't commit `.env`.
