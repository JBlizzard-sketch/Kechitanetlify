# Scaling & Cost Checklist
- Set Render autoscale min/max instances
- Configure Prometheus metrics for queue length, request latency, AI token usage
- Set alerts: AI daily cost > threshold, queue backlog > threshold
- Enable horizontal autoscaling for workers if queue backlog persists
