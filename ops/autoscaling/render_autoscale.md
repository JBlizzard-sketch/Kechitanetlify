# Render autoscaling hints
# Render handles scaling automatically for web services. Recommended settings:
# - Set min instances: 1, max instances: 4
# - Use health checks and CPU/memory thresholds to scale.

# For background worker use separate service with concurrency settings.
